import Repository from '../Repository';

class OrganizationRepository extends Repository {
  constructor() {
    super();
  }
  addOrganization = async (organization: number, userId: number) => {
    return null;
  };
  getOrganization = async (organizationId: number) => {
    return null;
  };
  updateOrganization = async (organizationId: number, organization: number) => {
    return null;
  };
  deleteOrganization = async (organizationId: number) => {
    return null;
  };
}
export default OrganizationRepository;
